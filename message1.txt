Perfect! Since you're using *Cloudera VM (on Oracle VirtualBox), Hive is already pre-installed and integrated with Hadoop, making it ideal for performing **descriptive analytics and basic statistics* using HiveQL.

---

## 🧪 *EXPERIMENT: Create Hive Database and Perform Descriptive Analytics (Basic Statistics)*

---

### 🎯 *Aim:*

To create a database and table in Apache Hive and perform basic statistical operations such as MIN, MAX, AVG, COUNT, and SUM.

---

### 🧠 *Theory:*

Apache Hive is a *data warehouse system* built on top of Hadoop for querying and analyzing large datasets stored in HDFS using SQL-like syntax called *HiveQL*.

Hive converts SQL-like queries into MapReduce jobs internally, making it scalable for big data processing.

---

## ⚙ *Steps to Perform in Cloudera VM*

### ✅ Step 1: Open Hive Shell

In your Cloudera VM terminal:

bash
hive


---

### ✅ Step 2: Create a Database

sql
CREATE DATABASE student_db;
USE student_db;


---

### ✅ Step 3: Create a Table

sql
CREATE TABLE marks (
    name STRING,
    subject STRING,
    marks INT
)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ','
STORED AS TEXTFILE;


---

### ✅ Step 4: Load Sample Data into Hive Table

1. First, create a CSV file on your VM:

bash
gedit marks.csv


Paste this data:


Alice,Math,85
Bob,Math,75
Alice,Science,90
Bob,Science,80
Charlie,Math,60
Charlie,Science,70


Save and close.

2. Move it to HDFS:

bash
hdfs dfs -mkdir /user/cloudera/hive_input
hdfs dfs -put marks.csv /user/cloudera/hive_input/


3. Load into the Hive table:

sql
LOAD DATA INPATH '/user/cloudera/hive_input/marks.csv' INTO TABLE marks;


---

### ✅ Step 5: Run Descriptive Analytics Queries

#### ➤ Get All Records

sql
SELECT * FROM marks;


#### ➤ Total Number of Rows

sql
SELECT COUNT(*) FROM marks;


#### ➤ Average Marks

sql
SELECT AVG(marks) FROM marks;


#### ➤ Minimum and Maximum Marks

sql
SELECT MIN(marks), MAX(marks) FROM marks;


#### ➤ Total Marks by Student

sql
SELECT name, SUM(marks) AS total_marks FROM marks GROUP BY name;


#### ➤ Average Marks by Subject

sql
SELECT subject, AVG(marks) AS avg_marks FROM marks GROUP BY subject;


---

### 📌 Optional: Describe Table

sql
DESCRIBE marks;


---

## ✅ *Conclusion:*

Successfully created a Hive database and table, inserted sample data, and performed descriptive analytics using HiveQL functions like COUNT, AVG, MIN, MAX, and SUM.

---

Would you like this formatted into a .docx for your practical file, or shall we proceed to a follow-up experiment like grouping, filtering, or joining in Hive?